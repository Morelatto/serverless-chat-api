# Chat API Configuration
# All environment variables use CHAT_ prefix

## Local Development Settings ##
CHAT_HOST=0.0.0.0
CHAT_PORT=8000
CHAT_LOG_LEVEL=INFO

## LLM Configuration ##
# Provider options: openrouter (default) or gemini
CHAT_LLM_PROVIDER=openrouter
# Set your API key for the selected provider
CHAT_GEMINI_API_KEY=
CHAT_OPENROUTER_API_KEY=
# Model for OpenRouter
CHAT_OPENROUTER_MODEL=google/gemma-7b-it:free

## Database Configuration ##
# Local: SQLite (default)
CHAT_DATABASE_URL=sqlite+aiosqlite:///./data/chat.db

# Production: DynamoDB (auto-detected in AWS Lambda)
# To force DynamoDB locally, uncomment:
# CHAT_DATABASE_URL=dynamodb://chat-interactions?region=us-east-1

## Cache Configuration ##
# Redis (optional - if configured, takes priority)
# CHAT_REDIS_URL=redis://localhost:6379

# Note: Cache strategy by environment:
# - Local: In-memory cache (no persistence)
# - AWS Lambda: DynamoDB cache (same table as database)
# - With Redis: Redis cache (when CHAT_REDIS_URL is set)

## AWS Configuration (for production) ##
CHAT_AWS_REGION=us-east-1
CHAT_DYNAMODB_TABLE=chat-interactions

## Rate Limiting ##
CHAT_RATE_LIMIT=60/minute

## Optional Settings ##
# CHAT_LOG_FILE=/var/log/chat-api.log
